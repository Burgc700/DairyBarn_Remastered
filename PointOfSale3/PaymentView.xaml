<UserControl x:Class="DairyBarn.PointOfSale.PaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DairyBarn.PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock FontSize="30" Text="Order Summary:" Margin="50"/>
        <TextBlock FontSize="13" Text="{Binding Path=Subtotal, StringFormat=' Subtotal: {0:C}'}" Margin="5"></TextBlock>
        <TextBlock FontSize="13" Text="{Binding Tax, StringFormat='Tax: {0:C}'}" Margin="5"></TextBlock>
        <TextBlock FontSize="13" Text="{Binding Total, StringFormat='Total: {0:C}'}" Margin="5"></TextBlock>
        <StackPanel Orientation="Horizontal" Margin="20">
            <TextBlock FontSize="13" Margin="5">Enter payment amount:</TextBlock>
            <TextBox Width="50" FontSize="13">
                <TextBox.Text>
                    <Binding Path="PaymentAmount" Delay="500" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" StringFormat='{}{0:C}'>
                        <Binding.ValidationRules>
                            <ExceptionValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBlock Text="{Binding ErrorMessage}"/>
        </StackPanel>
        <Button FontSize="13" Margin="10" IsEnabled="{Binding ValidPaymentAmount}" Click="FinalizePaymentClick">Finalize Payment and Print Receipt</Button>
        <TextBlock Text="{Binding Path=Change, StringFormat='Change: {0:C}'}" FontSize="13" Margin="10"></TextBlock>
    </StackPanel>
</UserControl>
