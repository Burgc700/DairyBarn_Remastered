<Window x:Class="DairyBarn.PointOfSale.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DairyBarn.PointOfSale"
        mc:Ignorable="d"
        Title="Dairy Barn" Height="550" Width="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="15"/>
                </Style>
            </Style.Resources>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5.5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="cancel" Grid.Column="0" Grid.Row="0" Content="Cancel Order" Margin="5"/>
            <Button x:Name="backToMenu" Grid.Column="1" Grid.Row="0" Content="Back to Menu" Margin="5"/>
            <Button x:Name="complete" Grid.Column="2" Grid.Row="0" Content="Complete Order" Margin="5"/>
        </Grid>

        <local:MainItemSelectionControl Grid.Column="0" Grid.Row="0"/>
        <local:OrderSummaryControl Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"/>
        
    </Grid>

    
</Window>
