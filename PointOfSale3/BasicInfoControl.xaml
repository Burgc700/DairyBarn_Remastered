<UserControl x:Class="DairyBarn.PointOfSale.BasicInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DairyBarn.PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        
            <TextBlock x:Name="NameofItem" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Grid.Row="0" Margin="10"/>
            <TextBlock x:Name="DescriptionofItem" Text="{Binding Description}" Grid.Row="1" VerticalAlignment="Center" TextWrapping="Wrap" Margin="10" FontSize="15"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" VerticalAlignment="Center" Margin="10">
            <TextBlock Text="Calories: " FontSize="15"/>
            <TextBlock x:Name="CalsofItem" Text="{Binding Calories}" Grid.Row="2" FontSize="15"/>
        </StackPanel>

    </Grid>
</UserControl>
